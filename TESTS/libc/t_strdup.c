#include "../test.h"

int		t_strdup(void)
{/*
	if (strcmp(ft_strdup("Eh gamin ! Reviens gamin !"), strdup("Eh gamin ! Reviens gamin !")))
		return (1);
	if (strcmp(ft_strdup(""), strdup("")))
		return (2);
*/
	return (-1);
}
